import { CurrencyZarPipe } from './currency-zar.pipe';

describe('CurrencyZarPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyZarPipe();
    expect(pipe).toBeTruthy();
  });
});
